# heic2png

A simple python script for converting a `.heic` file to `.png`.

# Setup

To install the required packages, run:
``` terminal
pip install -r requirements.txt
```

You can assign `heic2png.cmd` as the default program for `.heic` files in order to directly convert and then run the created `.png` file.